info:
  name: GET products categories
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/products/category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
