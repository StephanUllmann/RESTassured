info:
  name: Products-Categories
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/products/category"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
